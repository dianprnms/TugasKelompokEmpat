<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/login">

    <fragment
        android:id="@+id/login"
        android:name="com.example.tugaskelompokempat.Fragment.Login"
        android:label="fragment_login"
        tools:layout="@layout/fragment_login" >
        <action
            android:id="@+id/action_login_to_home2"
            app:destination="@id/home2" />
    </fragment>
    <fragment
        android:id="@+id/home2"
        android:name="com.example.tugaskelompokempat.Fragment.Home"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_home2_to_filmDetail"
            app:destination="@id/filmDetail" />
    </fragment>
    <fragment
        android:id="@+id/filmDetail"
        android:name="com.example.tugaskelompokempat.Fragment.FilmDetail"
        android:label="fragment_film_detail"
        tools:layout="@layout/fragment_film_detail" />
</navigation>